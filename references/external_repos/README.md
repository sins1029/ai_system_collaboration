# External Repositories

This directory is for local reference clones only.

The cloned repositories are excluded from the main Git history by `.gitignore`.
Keep mainline implementation in the top-level project modules.

