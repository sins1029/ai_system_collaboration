# 本次提交白名单

审计日期：2026-08-27
分支：`feature/single-center-task-runtime-v0.2`
基线 HEAD：`d49870b9d9fbee4b480100f4a64d74075b0e1a8b`

本白名单对应“三人协作开发前的第一版架构冻结与工程整理版本”。除“明确不提交”外，下列每个文件均属于第一版正式版本，建议进入本次提交。白名单共 73 个文件：14 个已跟踪修改、59 个新增文件、0 个删除文件。

# 本次计划提交

## 核心源码

- `src/datacenter_env/core/actuator.py`（Python 源码；既有执行器中文接口说明）
  - 保留单中心回归能力，修改仅为公共兼容接口中文化。
- `src/datacenter_env/core/environment.py`（Python 源码；既有单中心环境）
  - 保留 Architecture A 之外的既有单中心回归能力，修改仅为类说明中文化。
- `src/datacenter_env/exceptions.py`（Python 源码；公共异常）
  - 统一项目自身异常说明的中文口径。
- `src/datacenter_env/gym/wrappers.py`（Python 源码；Gym 扩展占位）
  - 保留已有包边界，中文化模块说明，不引入 Architecture B/C 实现。
- `src/datacenter_env/runtime/system.py`（Python 源码；既有运行时外观）
  - 保留既有回归路径并中文化公共类说明。
- `storage/database.py`（Python 源码；运行记录存储）
  - 移除个人机器 Git 绝对路径，改用 PATH 发现并提供缺失回退。
- `src/sustaincluster_mpc/__init__.py`（Python 源码；MPC 公共 API）
  - 暴露冻结的适配器和优化器接口。
- `src/sustaincluster_mpc/action_adapter.py`（Python 源码；动作适配）
  - 维护语义数据中心编号到上游动作位置的稳定映射与合法性校验。
- `src/sustaincluster_mpc/horizon_adapter.py`（Python 源码；时域适配）
  - 构造 H=1/H=4 状态，并隔离 deployable 与 oracle 信息。
- `src/sustaincluster_mpc/one_step_optimizer.py`（Python 源码；H=1 基线）
  - 提供同口径一步优化对照。
- `src/sustaincluster_mpc/rolling_horizon_optimizer.py`（Python 源码；H=4 MPC）
  - 实现第一版专家教师和优化基线。
- `src/sustaincluster_mpc/state_adapter.py`（Python 源码；状态适配）
  - 把 SustainCluster 可变对象转换为稳定只读调度快照。
- `src/sustaincluster_imitation/__init__.py`（Python 源码；模仿学习公共 API）
  - 暴露专家数据和 BC 主线接口。
- `src/sustaincluster_imitation/bc_policy.py`（Python 源码；BC 在线策略）
  - 加载默认检查点并输出统一语义动作。
- `src/sustaincluster_imitation/bc_trainer.py`（Python 源码；BC 训练）
  - 保留可复现的行为克隆训练能力，Demo 不调用训练。
- `src/sustaincluster_imitation/dataset_reader.py`（Python 源码；专家数据读取）
  - 校验并读取专家数据和 manifest。
- `src/sustaincluster_imitation/dataset_schema.py`（Python 源码；专家数据契约）
  - 固化任务、步骤、谱系和划分字段。
- `src/sustaincluster_imitation/dataset_writer.py`（Python 源码；专家数据写入）
  - 以受控格式持久化本地专家数据，数据本体不进 Git。
- `src/sustaincluster_imitation/environment_factory.py`（Python 源码；第三方环境工厂）
  - 只读加载 SustainCluster，解析 workload 并恢复调用方工作目录。
- `src/sustaincluster_imitation/expert_collector.py`（Python 源码；专家采集）
  - 将 H=4 MPC 决策写入无泄漏专家数据契约。
- `src/sustaincluster_imitation/feature_encoder.py`（Python 源码；特征编码）
  - 按稳定语义顺序编码 BC 输入与动作 mask。
- `src/sustaincluster_imitation/forecast_baseline.py`（Python 源码；因果预测）
  - 提供只依赖历史信息的部署可用预测基线。
- `src/sustaincluster_imitation/paths.py`（Python 源码；路径解析）
  - 实现 CLI、`SUSTAINCLUSTER_ROOT`、默认目录的路径优先级。
- `src/sustaincluster_imitation/sac_weight_bridge.py`（Python 源码；权重桥接）
  - 保留 BC 到同结构 RL actor 的初始化能力，不把 vanilla SAC 设为默认路线。
- `src/sustaincluster_imitation/split_dataset.py`（Python 源码；数据划分）
  - 强制 seed/episode 互斥并隔离 oracle。
- `src/sustaincluster_imitation/synthetic_scenarios.py`（Python 源码；合成场景）
  - 支持确定性边界回归和小型研究复现。

## 配置

- `.gitattributes`（Git 配置；文本与二进制属性）
  - 固定文本 LF，并将 checkpoint、Parquet、PDF 等标记为二进制。
- `.gitignore`（Git 配置；提交边界）
  - 排除外部仓库、大数据、虚拟环境、缓存、数据库和实验中间产物。
- `pyproject.toml`（项目配置；包、依赖与 pytest）
  - 声明 Python 3.10+、核心/研究/测试依赖和受控测试目录。
- `configs/sustaincluster_mpc/h1_expert.yaml`（YAML 配置；H=1 对照）
  - 固化同口径一步优化基线。
- `configs/sustaincluster_mpc/h4_expert.yaml`（YAML 配置；H=4 专家）
  - 固化第一版默认 MPC 专家参数。
- `configs/sustaincluster_imitation/dataset.yaml`（YAML 配置；专家数据）
  - 固化数据变体、seed 划分和 oracle 隔离。
- `configs/sustaincluster_imitation/bc_train.yaml`（YAML 配置；BC 训练）
  - 固化已验证行为克隆训练参数。
- `configs/sustaincluster_imitation/sac_warm_start.yaml`（YAML 配置；负结果复现）
  - 保留历史 vanilla SAC 对照，但不作为默认主线。

## 脚本

- `scripts/clone_reference_repos.ps1`（PowerShell；参考仓库下载）
  - 移除个人 Git 路径，支持 PATH 或显式 `-GitPath`。
- `scripts/setup_env.ps1`（PowerShell；环境安装）
  - 使用 Python 3.10+、上游依赖和项目 extras 创建可复现环境。
- `scripts/run_tests.py`（Python；统一测试入口）
  - 调用 pytest，并显式传递项目根目录与 `src`，无需依赖当前 shell 的 PYTHONPATH。
- `scripts/run_sustaincluster_demo.py`（Python；最小闭环入口）
  - 统一运行 5–20 步 MPC 或 BC，不训练、不写数据。
- `scripts/compare_single_center_runs.py`（Python；既有单中心比较）
  - 保留旧路径兼容，修改仅为公共辅助函数说明中文化。

## 测试

- `tests/test_database.py`（Python 测试；数据库路径回退）
  - 覆盖系统找不到 Git 时返回空提交号的回归。
- `tests/test_sustaincluster_imitation.py`（Python 测试；模仿学习集成）
  - 覆盖路径、schema、划分、特征、BC、checkpoint 和 Demo 契约。
- `reports/sustaincluster_mpc/test_one_step_scheduler.py`（Python 测试；H=1）
  - 覆盖一步调度动作、资源和闭环契约。
- `reports/sustaincluster_mpc/test_rolling_horizon_optimizer.py`（Python 测试；H=4）
  - 覆盖滚动时域状态、计划、边界和退化行为。

## 文档

- `README.md`（Markdown；项目首页）
  - 给出 Architecture A 定位、安装、Demo、测试、目录与三人协作边界。
- `docs/使用说明.md`（Markdown；用户文档）
  - 说明上游仓库、workload、checkpoint、数据、命令与常见错误。
- `docs/架构说明.md`（Markdown；架构文档）
  - 固化 A/B/C 决策、六层边界和信息泄漏规则。
- `docs/开发说明.md`（Markdown；协作文档）
  - 固化模块所有权、中文规范、测试门槛和 feature branch + PR 流程。
- `artifacts/sustaincluster_imitation/README.md`（Markdown；模型说明）
  - 记录默认 BC 检查点来源、用途和排除其他产物的原则。
- `reports/architecture_audit_20260818/architecture_decision_report.md`（Markdown；综合架构证据）
  - 保留一份合并后的架构审计与路线决策，替代 11 份重复子报告。
- `reports/pre_git_release/01_架构决策.md`（Markdown；冻结记录）
  - 记录 Architecture A 冻结结论和当前发布验证。
- `reports/pre_git_release/release_candidate_report.md`（Markdown；发布候选总报告）
  - 汇总架构、代码、依赖、测试、Git 边界与已知问题。
- `reports/pre_git_release/10_本次提交白名单.md`（Markdown；提交审计）
  - 逐文件记录本次允许暂存和明确排除的内容。
- `reports/sustaincluster_imitation/bc_closed_loop_report.md`（Markdown；BC 闭环证据）
  - 保留 BC 与 MPC 的真实闭环评价结论。
- `reports/sustaincluster_imitation/bc_offline_evaluation_report.md`（Markdown；BC 离线证据）
  - 保留行为克隆准确率与分任务指标。
- `reports/sustaincluster_imitation/expert_dataset_report.md`（Markdown；专家数据证据）
  - 记录数据规模、求解失败和划分规则。
- `reports/sustaincluster_imitation/final_validation_report.md`（Markdown；最终验证）
  - 汇总专家数据、BC 与 SAC warm-start 的已完成结论。
- `reports/sustaincluster_imitation/h1_h4_fair_comparison.md`（Markdown；优化基线比较）
  - 保留 H=1/H=4 同口径结果，约束对 H=4 收益的表述。
- `reports/sustaincluster_imitation/sac_warm_start_report.md`（Markdown；负结果证据）
  - 记录 vanilla SAC 更新导致专家行为漂移的依据。
- `reports/sustaincluster_mpc/action_encoding_audit.md`（Markdown；动作接口审计）
  - 记录上游动态动作映射与语义动作契约。
- `reports/sustaincluster_mpc/one_step_closed_loop_report.md`（Markdown；H=1 闭环证据）
  - 记录一步优化闭环行为和边界。
- `reports/sustaincluster_mpc/rolling_horizon_report.md`（Markdown；H=4 设计证据）
  - 记录滚动时域模型、目标和实验结论。
- `reports/sustaincluster_mpc/state_interface_audit.md`（Markdown；状态接口审计）
  - 记录 SustainCluster 状态读取边界。
- `reports/sustaincluster_mpc/temporal_model_audit.md`（Markdown；时间模型审计）
  - 记录时域推进、预测信息和泄漏边界。

## 其他

- `artifacts/sustaincluster_imitation/bc_actor_seed_11.pt`（PyTorch checkpoint；BC 推理）
  - BC Demo 必需，大小 517589 bytes（0.494 MiB），SHA256：`124a86cce281c834b45e3a734015ec5fa79520babc7c19d01fd167993a5f64ab`。
- `reports/sustaincluster_imitation/augment_primary_dataset.py`（Python 复现脚本；专家数据扩充）
  - 保留已完成数据构建的可复现入口，输出数据不提交。
- `reports/sustaincluster_imitation/generate_expert_dataset.py`（Python 复现脚本；专家采集）
  - 允许团队从冻结配置重建本地专家数据。
- `reports/sustaincluster_imitation/run_bc_closed_loop.py`（Python 复现脚本；BC 闭环）
  - 复现正式闭环评价和指标汇总。
- `reports/sustaincluster_imitation/run_fair_comparison.py`（Python 复现脚本；H=1/H=4/BC 比较）
  - 复现同 seed、同窗口的公平比较。
- `reports/sustaincluster_imitation/run_sac_warm_start.py`（Python 复现脚本；SAC 负结果）
  - 保留已完成负结果的复现能力，不进入默认 Demo。
- `reports/sustaincluster_imitation/train_bc.py`（Python 复现脚本；BC 训练）
  - 复现默认 checkpoint 的训练流程，训练产物不提交。
- `reports/sustaincluster_mpc/run_one_step_closed_loop.py`（Python 复现脚本；H=1 闭环）
  - 复现一步优化基线。
- `reports/sustaincluster_mpc/run_rolling_horizon_experiments.py`（Python 复现脚本；H=4 实验）
  - 复现冻结的滚动时域实验矩阵。
- `reports/sustaincluster_mpc/stress_scenarios.py`（Python 复现脚本；边界场景）
  - 提供容量、负载和前瞻压力场景。

# 明确不提交

以下 20 个当前未跟踪文件不进入本次暂存；它们是已被综合报告覆盖的重复分章、时效性较强的执行计划/审计分表，或临时依赖说明。

- `reports/architecture_audit_20260818/01_project_research_logic.md`
  - 内容已并入综合架构决策报告。
- `reports/architecture_audit_20260818/02_current_implementation.md`
  - 当前实现快照会快速过时，核心边界已进入正式架构文档。
- `reports/architecture_audit_20260818/03_evidence_summary.md`
  - 证据摘要已由综合架构决策报告和正式研究报告覆盖。
- `reports/architecture_audit_20260818/04_architecture_A_direct_RL.md`
  - Architecture A 结论已进入正式架构说明。
- `reports/architecture_audit_20260818/05_architecture_B_event_trigger_mpc.md`
  - Architecture B 只保留正式文档中的扩展边界。
- `reports/architecture_audit_20260818/06_architecture_C_hierarchical_rl_mpc.md`
  - Architecture C 只保留正式文档中的研究候选说明。
- `reports/architecture_audit_20260818/07_architecture_evidence_matrix.md`
  - 证据矩阵已被综合报告吸收。
- `reports/architecture_audit_20260818/08_open_questions.md`
  - 临时问题清单不进入第一版正式提交。
- `reports/architecture_audit_20260818/09_minimum_discriminating_experiments.md`
  - 属于暂停中的后续实验规划，不进入本轮。
- `reports/architecture_audit_20260818/10_recommended_project_route.md`
  - 推荐路线已固化到 README 与架构说明。
- `reports/architecture_audit_20260818/11_20_day_three_person_execution_plan.md`
  - 时效性执行计划由开发说明中的长期角色边界替代。
- `reports/pre_git_release/02_当前目录审计.md`
  - 本地目录快照属于临时审计材料。
- `reports/pre_git_release/03_核心文件清单.md`
  - 被本白名单和正式架构说明覆盖。
- `reports/pre_git_release/04_删除候选清单.md`
  - 临时清理建议不属于正式版本。
- `reports/pre_git_release/05_git文件清单.md`
  - 被本白名单和 staged diff 精确替代。
- `reports/pre_git_release/06_依赖审计.md`
  - 时点依赖快照由 `pyproject.toml` 和发布候选总报告覆盖。
- `reports/pre_git_release/07_测试报告.md`
  - 时点测试明细由发布候选总报告和提交前审查记录覆盖。
- `reports/pre_git_release/08_三人协作建议.md`
  - 长期协作边界已进入 `docs/开发说明.md`。
- `reports/pre_git_release/09_发布前检查清单.md`
  - 属于一次性审计清单，不进入正式版本。
- `reports/sustaincluster_mpc/mpc-extra-requirements.txt`
  - 临时阶段依赖说明，权威依赖已进入 `pyproject.toml`。

以下目录或文件类型均被 `.gitignore` 排除，不属于当前未跟踪清单，也不进入本次提交：

- `references/external_repos/*`
  - 五个只读外部参考仓库，SustainCluster 保持独立 clean 仓库。
- `data/processed/`、`data/raw/`、`data/workload/`、`data/db/*`
  - 原始 workload、专家 Parquet、oracle、replay 和 SQLite 均为大数据或本地产物。
- `artifacts/sustaincluster_imitation/*`（白名单 checkpoint 与 README 除外）
  - 其他 seed checkpoint、训练日志和中间模型可再生成。
- `reports/**/*.csv`、`reports/**/*.json`、`reports/**/*.parquet`、`reports/**/*.pdf`、`reports/**/*.tex`
  - 批量实验数据、渲染结果和中间产物不进入源码提交。
- `.venv*/`、`.runtime/`、`.cache/`、`__pycache__/`、`.pytest_cache/`
  - 本地环境、运行时和缓存不可移植。
- `ppt/`、`.vscode/`、`.idea/`、`tmp/`、`*.log`、`*.sqlite*`
  - 汇报工作目录、IDE 配置和临时文件不属于第一版主线。
